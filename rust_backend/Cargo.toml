[package]
name = "ods-accelerate"
version = "0.1.0"
edition = "2021"
description = "Rust acceleration for open-daily-stock backtest engine"

[lib]
name = "ods_accelerate"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
